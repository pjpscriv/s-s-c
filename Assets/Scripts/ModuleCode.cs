using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ModuleCode : MonoBehaviour
{

    public int moduleID;
    // 0 = AI
    // 1 = Temp
    // 2 = Electric Plug
    // 3 = Electric Charger
    // 4 = Shield
    // 5 = Potion, Water or Sink
    // 6 = Portal

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
